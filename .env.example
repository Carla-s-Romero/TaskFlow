# Copy this file to .env before running `docker compose up`
# Used by docker-compose.yml for the MySQL container and the backend service

MYSQL_ROOT_PASSWORD=root
DB_NAME=taskflow
DB_USER=root
DB_PASSWORD=root
